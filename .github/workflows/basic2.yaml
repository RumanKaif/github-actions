---
name: CI
on:
 push:
    branches:
        - main
jobs:
    build:
        runs-on: ubuntu:latest
        steps:
            - name: checkout code  
              uses: actions/checkout@v2

            - name: run a script 
              uses: echo "hello world"
          
